import 'package:flutter/material.dart';

class ProductEventScreen extends StatelessWidget {
  const ProductEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('ProductEventScreen'));
  }
}
